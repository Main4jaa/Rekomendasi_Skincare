import { ChevronDown } from 'lucide-react';
export default function SymptomGroup({ category, symptoms, selected, onToggle }){ return <details open className="rounded-xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><summary className="flex cursor-pointer items-center justify-between p-3 font-semibold"><span>{category}</span><ChevronDown size={18}/></summary><div className="grid gap-2 p-3 pt-0 md:grid-cols-2">{symptoms.map(s=><label key={s.code} className={`flex cursor-pointer gap-3 rounded-xl border p-3 ${selected.includes(s.code)?'border-primary-500 bg-primary-50 dark:bg-primary-950':'border-slate-200 dark:border-slate-800'}`}><input type="checkbox" checked={selected.includes(s.code)} onChange={()=>onToggle(s.code)} className="mt-1"/><span><span className="font-semibold">{s.code}</span> {s.description}<span className="ml-2 text-xs text-slate-500">CF {s.cf.toFixed(2)}</span></span></label>)}</div></details>; }
