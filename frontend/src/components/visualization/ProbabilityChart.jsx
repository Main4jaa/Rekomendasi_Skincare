import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
export default function ProbabilityChart({ probabilities={} }){ const data=Object.entries(probabilities).map(([name,value])=>({name, probability:Number(value)})); return <div className="h-64 w-full"><ResponsiveContainer><BarChart data={data} layout="vertical" margin={{left:50,right:20}}><CartesianGrid strokeDasharray="3 3"/><XAxis type="number" domain={[0,1]} tickFormatter={v=>`${Math.round(v*100)}%`}/><YAxis dataKey="name" type="category" width={120}/><Tooltip formatter={v=>`${Math.round(v*100)}%`}/><Bar dataKey="probability" radius={[0,8,8,0]} /></BarChart></ResponsiveContainer></div>; }
