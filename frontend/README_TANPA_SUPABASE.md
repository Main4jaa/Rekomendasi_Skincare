# Frontend Tanpa Supabase

Frontend ini langsung memakai backend FastAPI lokal.

Pastikan file `.env` berisi:

```text
VITE_API_URL=http://127.0.0.1:8000
```

Jalankan:

```powershell
cd frontend
npm install
npm run dev
```

Buka:

```text
http://localhost:5173
```
